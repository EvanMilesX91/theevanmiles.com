// This footer component is no longer used
// The homepage now has its own built-in footer
// Keeping this file to prevent import errors in other pages
export default function Footer() {
  return null;
}